{
  "name": "planning-joker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "partykit"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:party": "npm --workspace partykit run dev",
    "build": "npm --workspace frontend run build",
    "test": "npm --workspace partykit run test",
    "typecheck": "npm --workspace frontend run typecheck && npm --workspace partykit run typecheck"
  }
}
